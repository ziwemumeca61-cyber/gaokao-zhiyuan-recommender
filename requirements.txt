# Streamlit Community Cloud 部署依赖（运行时）
# 与 pyproject.toml 的 [project].dependencies 保持一致，外加可选的 Word/PDF 导出依赖。
# 页面通过 sys.path.insert 加入 src/，故云端无需 editable 安装本包。
streamlit>=1.33
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
plotly>=5.18
openai>=1.30

# 可选导出（缺失时自动降级为 Markdown 导出，可按需删除以加快部署）
python-docx>=1.1
reportlab>=4.0
